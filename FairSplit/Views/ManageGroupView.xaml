<UserControl x:Class="FairSplit.Views.ManageGroupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FairSplit.Views" xmlns:viewmodels="clr-namespace:FairSplit.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:ManageGroupViewModel}"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition MaxWidth="400"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,10,10,10">
            <Viewbox MaxHeight="150" Stretch="Uniform" Margin="50 5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <TextBlock Text="{Binding OldName}" Foreground="#D3D3D3"/>
            </Viewbox>
        </StackPanel>

        <Border Grid.Column="1" Background="#FF131313" Width="10" CornerRadius="5" HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="0,10,0,10"/>


        <Grid Grid.Column="3" HorizontalAlignment="Stretch" Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition MaxHeight="50" />
                <RowDefinition Height="*"/>
                <RowDefinition MaxHeight="50"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" HorizontalAlignment="Center" Text="Name" FontSize="20" Foreground="#D3D3D3" Margin="10 10"/>

            <TextBox Grid.Row="1" Text="{Binding NameBoxText}"/>

            <TextBlock Grid.Row="2" HorizontalAlignment="Center" Text="Members" FontSize="20" Foreground="#D3D3D3" Margin="10 10"/>
            
            <DataGrid Grid.Row="3"
                      ItemsSource="{Binding Persons}"
                      CanUserAddRows="True"
                      CanUserDeleteRows="True"
                      HorizontalContentAlignment="Center"
                      AutoGenerateColumns="False"
                      HeadersVisibility="None"
                      GridLinesVisibility="None"
                      IsReadOnly="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Name}" Width="370">
                        <DataGridTextColumn.EditingElementStyle>
                            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                <Setter Property="Background" Value="Transparent"/>
                            </Style>
                        </DataGridTextColumn.EditingElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!--<Button Background="#FF212D1D" 
                    Grid.Row="4" 
                    Command="{Binding BackCommand}" 
                    HorizontalAlignment="Stretch" 
                    Content="New Member"  
                    Margin="0,-10,0,0"/>-->

            <Grid Grid.Row="4" HorizontalAlignment="Stretch" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Command="{Binding BackCommand}" HorizontalAlignment="Stretch" Content="Back"  Margin="0,0,5,0"/>
                <Button Grid.Column="1" Command="{Binding DeleteCommand}" HorizontalAlignment="Stretch" Content="Delete" Margin="5,0,5,0" Background="#FF5B1818"/>


            </Grid>

        </Grid>
    </Grid>
</UserControl>
