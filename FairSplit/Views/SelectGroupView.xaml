<UserControl x:Class="FairSplit.Views.SelectGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FairSplit.Views" xmlns:viewmodels="clr-namespace:FairSplit.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:SelectGroupViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition MaxWidth="400"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,10,10,10">
            <Viewbox MaxHeight="150" Stretch="Uniform" Margin="50 5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <TextBlock Text="FairSplit" Foreground="#D3D3D3"/>
            </Viewbox>
            <Viewbox MaxHeight="20" Stretch="Uniform" Margin="80 5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <TextBlock Text="By Marcel Nadzam" Foreground="#D3D3D3" FontWeight="Bold"/>
            </Viewbox>
        </StackPanel>

        <Border Grid.Column="1" Background="#FF131313" Width="10" CornerRadius="5" HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="0,10,0,10"/>


        <Grid Grid.Column="3" HorizontalAlignment="Stretch" Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition MaxHeight="50" />
                <RowDefinition Height="*"/>
                <RowDefinition MaxHeight="50"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" HorizontalAlignment="Center" Text="Your Groups" FontSize="20" Foreground="#D3D3D3" Margin="10 10"/>

            <ListView Grid.Row="1" ItemsSource="{Binding Groups}" SelectionMode="Single" SelectedItem="{Binding SelectedGroup}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" FontSize="18"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid Grid.Row="2" HorizontalAlignment="Stretch" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Command="{Binding SelectGroupCommand}" HorizontalAlignment="Stretch" Content="Select"  Margin="0,0,5,0"/>
                <Button Grid.Column="1" Command="{Binding ManageGroupCommand}" HorizontalAlignment="Stretch" Content="Manage" Margin="5,0,5,0"/>
                <Button Grid.Column="2" Command="{Binding NewGroupCommand}" HorizontalAlignment="Stretch" Content="New" Margin="5,0,0,0"/>


            </Grid>

        </Grid>
    </Grid>
</UserControl>
