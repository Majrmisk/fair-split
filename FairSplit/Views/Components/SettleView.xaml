<UserControl x:Class="FairSplit.Views.Components.SettleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FairSplit.Views.Components" xmlns:viewmodels="clr-namespace:FairSplit.ViewModels.Components" d:DataContext="{d:DesignInstance Type=viewmodels:SettleViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>
        <DataGrid ItemsSource="{Binding Transactions}" 
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    HorizontalContentAlignment="Center"
                    Margin="0 8 8 8"
                    Grid.Row="1"
                    ColumnWidth="*"
                    CanUserReorderColumns="False"
                    CanUserSortColumns="False"
                    CanUserResizeColumns="False"
                    GridLinesVisibility="None"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn 
                    Header="Select" 
                    Binding="{Binding IsSelected}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount}" />
                <DataGridTextColumn Header="Payer" Binding="{Binding NameOfPayer}" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" />
                <DataGridTextColumn Header="Date" Binding="{Binding TransactionTime}" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <DataGrid ItemsSource="{Binding CalculatedPayments}" 
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    HorizontalContentAlignment="Center"
                    Margin="0 8 8 8"
                    ColumnWidth="*"
                    CanUserReorderColumns="False"
                    CanUserSortColumns="False"
                    CanUserResizeColumns="False"
                    GridLinesVisibility="None"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    Grid.ColumnSpan="2">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="From" Binding="{Binding From}" Width="*"/>
                    <DataGridTextColumn Header="To" Binding="{Binding To}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button Command="{Binding DoneCommand}" Grid.Row="1" Margin="0 0 4 8" Content="Done" Background="#FF1A371D"/>
            <Button Command="{Binding CancelCommand}" Grid.Row="1" Grid.Column="1" Margin="4 0 8 8" Content="Cancel"/>

        </Grid>

        
    </Grid>
</UserControl>
