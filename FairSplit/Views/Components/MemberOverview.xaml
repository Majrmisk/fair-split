<UserControl x:Class="FairSplit.Views.Components.MemberOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FairSplit.Views.Components" xmlns:viewmodels="clr-namespace:FairSplit.ViewModels.Components" d:DataContext="{d:DesignInstance Type=viewmodels:MemberOverViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150" />
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button HorizontalAlignment="Stretch" 
                    VerticalAlignment="Stretch" 
                    HorizontalContentAlignment="Left"
                    VerticalContentAlignment="Bottom"
                    Margin="0 8 4 4"
                    FontSize="25"
                    FontWeight="DemiBold"
                    Command="{Binding NewTransactionCommand}">
                <TextBlock>
                    <Run Text="New"/>
                    <LineBreak/>
                    <Run Text="Transaction"/>
                </TextBlock>
            </Button>

            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Label Grid.Row="1" Style="{StaticResource BasicLabelStyle}" 
                       Content="Last Month" FontSize="15"/>
                <Label Grid.Row="2" Style="{StaticResource BasicLabelStyle}" 
                       Content="All Time" FontSize="15"/>
                <Label Grid.Column="1" Style="{StaticResource BasicLabelStyle}" 
                       Content="Paid" FontSize="15"/>
                <Label Grid.Column="2" Style="{StaticResource BasicLabelStyle}" 
                       Content="Owed" FontSize="15"/>
                <Label Grid.Column="1" Grid.Row="1" Style="{StaticResource WithBackgroundLabelStyle}" 
                       Content="{Binding PaidMonth}" FontSize="18" Margin="4"/>
                <Label Grid.Column="1" Grid.Row="2" Style="{StaticResource WithBackgroundLabelStyle}" 
                       Content="{Binding Paid}" FontSize="18" Margin="4"/>
                <Label Grid.Column="2" Grid.Row="1" Style="{StaticResource WithBackgroundLabelStyle}" 
                       Content="{Binding OwedMonth}" FontSize="18" Margin="4 4 8 4"/>
                <Label Grid.Column="2" Grid.Row="2" Style="{StaticResource WithBackgroundLabelStyle}" 
                       Content="{Binding Owed}" FontSize="18" Margin="4 4 8 4"/>
            </Grid>
        </Grid>


        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label Style="{StaticResource BasicLabelStyle}" 
                   Content="Your Transactions"
                   HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch"/>

            <DataGrid  Grid.Row="1" 
                        ItemsSource="{Binding Transactions}"
                        CanUserAddRows="False"
                        CanUserDeleteRows="False"
                        HorizontalContentAlignment="Center"
                        AutoGenerateColumns="False"
                        HeadersVisibility="Column"
                        GridLinesVisibility="None"
                        IsReadOnly="True"
                        Margin="0 0 8 8"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount}" Width="auto"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="auto"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding TransactionTime}" Width="auto"/>
                    <DataGridTextColumn Header="Settled" Binding="{Binding PayedOff}" Width="auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
